function toggleMenu() {
    var toggleList = document.getElementById('toggleMenuList');
    toggleList.classList.toggle('show');
};

function ballondor(){
    var ballondors= document.querySelectorAll("imgone");
   onclick="ballondor"
};

function redirectToWebsite(url) {
    window.location.href = url;
}